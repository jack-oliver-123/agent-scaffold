# Compose derived projects from declarative profiles

Stack Profiles will be declarative overlays selected one at a time during Baseline Initialization, with their own templates and tests rather than direct edits to Core Baseline sources. Unselected profiles and generation resources will not remain in a Derived Project.
