# Upgrade derived projects with explicit migrations

Derived Projects will record their baseline version and receive later baseline changes only through explicit, reviewable migrations with release notes. The baseline will never continuously synchronize or silently overwrite files owned by a Derived Project.
