# Keep upstream skills immutable

Upstream Skills will remain content-addressed and unmodified; compatibility changes will be implemented as Baseline-owned Skills or agent-specific wrappers selected by the Skill Manifest. This preserves auditable provenance and prevents local fixes from being silently overwritten during upstream synchronization.
