# Distribute through an interactive Project Creator

The primary creation path will be the public `create-agent-scaffold` npm package rather than cloning the template repository. One short command will start an interactive, checkbox-driven selection from the extensible Supported Agent set, then atomically create and validate a new Derived Project; explicit flags will preserve non-interactive automation.
