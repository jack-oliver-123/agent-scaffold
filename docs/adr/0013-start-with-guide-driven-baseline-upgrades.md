# Start with guide-driven baseline upgrades

During the `0.x` series, each Baseline Upgrade will be delivered as a version-specific Migration Guide with optional reviewable scripts rather than a new upgrade CLI. Real migrations will establish whether automation is justified before the project accepts another long-lived tool and distribution surface.
