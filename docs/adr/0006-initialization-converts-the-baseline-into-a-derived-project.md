# Initialization converts the baseline into a derived project

Baseline Initialization will be a guarded, one-time conversion that replaces baseline-authoring documentation and removes maintenance-only assets before recording Baseline Provenance. It will require a clean, uninitialized Git repository and will refuse to overwrite an existing Derived Project.
