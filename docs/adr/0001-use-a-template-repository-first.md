# Use a template repository first

Status: Superseded by ADR-0016.

The first release will be a reusable repository template for greenfield projects, rather than a generator CLI or an installer for existing repositories. A concrete, directly usable template gives us a stable product to validate before taking on generation, merging, and upgrade workflows.
