# Use Node.js and pnpm as the baseline toolchain

The first release will use Node.js and pnpm for baseline initialization, synchronization, validation, and release automation, while keeping Core Baseline guidance independent of application technology. A different distribution mechanism will be reconsidered only when a real non-Node Stack Profile demonstrates that this tooling requirement is unacceptable.
