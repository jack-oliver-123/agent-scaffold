# Use a minimal TypeScript and Node.js toolchain

The first Stack Profile will target the current supported Node.js LTS with pinned pnpm, ESM, strict TypeScript, Biome, Vitest, and direct `tsc` output. This provides cross-platform formatting, linting, type checking, testing, and builds without committing the baseline to an application framework or bundler.
