# Replace incompatible skills with stable baseline-owned interfaces

When an Upstream Skill's public workflow is valuable but its implementation is not portable across supported agents, the Default Skill Set may expose a compatible Baseline-owned Skill under the established name while retaining upstream provenance separately. The first such replacement will preserve `$grill-with-docs` without projecting the tool-specific upstream implementation.
