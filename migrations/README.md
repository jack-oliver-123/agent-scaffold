# Baseline Migrations

Each post-0.1.0 release that changes a Derived Project contract must add a version-specific
Migration Guide here. Prefer explicit reviewable steps; add a script only when the migration is
deterministic and safely repeatable.
