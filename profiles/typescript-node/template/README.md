# {{PROJECT_NAME}}

{{DESCRIPTION}}

## Requirements

- Node.js 24 LTS
- Corepack

## Commands

- `corepack pnpm install` installs the pinned dependencies.
- `corepack pnpm dev` runs the TypeScript entry point.
- `corepack pnpm test` runs the test suite once.
- `corepack pnpm check` runs every required quality check.

Agent 共享约束位于 `AGENTS.md`。Claude Code 通过 `CLAUDE.md` 导入同一份约束。
